import React from "react";
import "./style.css";

export const Salir = ({ className }) => {
  return (
    <div className={`salir ${className}`}>
      <div className="text-wrapper">Salir</div>
    </div>
  );
};
